// How to convert string character into ASCII code. 
char = "Alook";
console.log(char.charCodeAt(0));
console.log(char.charCodeAt(1));


// Length of empty array
const array = [];
console.log(array.length);

// undefined means false, and if we apply ! operator it will become true.
if(undefined)
	console.log(1);